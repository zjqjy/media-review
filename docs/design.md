---
type: 调研
tags: [自媒体, 复盘, Claude技能, B站, 项目设计]
status: 沉淀
created: 2026-09-16
sources:
  - https://github.com/SocialSisterYi/bilibili-API-collect
  - https://github.com/ZJU-REAL/Easel
  - https://developer.open-douyin.com/
---

# 自媒体复盘系统 · 设计文档

> 需求原文见 [[06_归档/2026-09-16_自媒体复盘系统搭建|自媒体复盘系统搭建]]（已归档）。项目入口 [[CLAUDE.md|项目 CLAUDE]]。

## 一、痛点与目标

当前视频发出去之后靠人工复盘，能力有限、无反馈、不知道优化点在哪。目标：做成 Claude Code skill，发布后自动取数、按本 vault 的自媒体方法论归因、产出可执行的改动清单并回写知识库。

## 二、调研结论（2026-09-16）

### 平台数据获取风险

| 平台 | 方案 | 风险 | 结论 |
|---|---|---|---|
| B站 | 自己账号 Cookie 调创作中心接口（bilibili-API-collect 有"创作中心→统计与数据"文档，配套 Python 库 `bilibili-api`） | 低——取自己的数据，非爬虫 | ✅ 全自动 |
| 抖音 | 官方开放平台：「视频查询」+「视频数据」能力，OAuth 取数 | 无风险但流程繁琐（实名+建应用+申请能力） | ⚠️ M3 备选，MVP 手动 |
| 小红书 | 无官方个人 API；爬虫撞"阿瑞斯"风控（AI 实时预审，轻则限流重则封号） | 高 | ❌ 不爬，永久手动 |

### GitHub 现成轮子

| 项目 | 评估 |
|---|---|
| [Easel](https://github.com/ZJU-REAL/Easel)（浙大） | 最接近（AI 社媒智能体，含内容效果分析），但是独立平台，重、不联动本知识库 → 不采用 |
| [MediaCrawler](https://github.com/NanmiCoder/MediaCrawler)（30K★） | 爬别人的公开数据，非复盘自己账号，且有风控风险 → 不采用 |
| xiaobei | 企业向运营套件，有数据复盘工作流，独立平台 → 不采用 |

**结论**：数据获取层站在 bilibili-API-collect 肩上；"联动知识库 + 方法论归因"无人能做，自建 skill。

## 三、已确认决策（2026-09-16，用户逐项拍板）

1. **复盘粒度**：先做单条内容复盘（发布后 D+3/D+7/D+30 三个观察点），数据攒起来后做账号周期复盘
2. **数据输入**：B站全自动 + 抖音/小红书手动粘贴（抖音官方 API 以后有需要再接）
3. **触发方式**：手动喊 `/media-review`，skill 自动清算所有"今天到了观察点"的内容，无状态零维护
4. **写回机制**：报告 + 半自动写回——报告末尾给"建议写回"清单，用户确认后才写入选题池/复用片段/对标。理由：方法论铁律"选题验证不能 AI 替代"，数据好≠选题被验证
5. **期驱动**（2026-09-16 实测反馈新增）：用户三平台统一发布，复盘以"期"为单位——B站到期为主轴，抖音/小红书由 skill **主动问**+收集窗截图（登记表降级为可选线索），AI 按标题配期
6. **汇总报告**：一期一份报告（`YYYY-MM-DD_期数_标题.md`），三平台数据同表对比 + 跨平台结论，不再按平台各写各的
7. **改进实验闭环**：报告"下条改动"逐条登记 `_改进实验.md`（改动/假设/可判定的验证标准），下期复盘先核对：有效→确认后沉淀复用片段+标已验证；无效→标注分析。这是方法论"验证机制"的实例化

## 四、架构

```
触发：/media-review
        │
        ▼
┌─ 代码仓 D:\DeskTop\WorkSpace\Code\media-review\ ──┐
│ skill/media-review/SKILL.md       流程+诊断框架     │
│ skill/media-review/scripts/fetch_bili.py  B站取数  │
│ scripts/screenshot_tray.py       截图收集窗（复盘时启动）  │
│ install.ps1                       安装 skill 到 ~/.claude/skills/ │
└──────────────────────────────────────────┘
        │ 读写（路径走配置）
        ▼
vault（D:\DeskTop\WorkSpace\obsidian\）
├─ 20_自媒体/复盘/_config_local.json  Cookie+vault路径（# sensitive，gitignore）
├─ 20_自媒体/复盘/_发布登记.md          可选线索（B站驱动为主，登记非必须）
├─ 20_自媒体/复盘/_改进实验.md          跨期改进闭环登记（改动→假设→验证→沉淀）
├─ 20_自媒体/复盘/_截图收件/<期数>/      分平台分期数（收集窗落盘，AI后整理兜底）
└─ 20_自媒体/复盘/YYYY-MM-DD_期数_标题.md  汇总报告（一期一份，观察点追加更新）
```

### 代码仓结构

```
media-review/
├── CLAUDE.md          # 开发上下文
├── README.md          # GitHub 门面
├── install.ps1        # 安装/更新 skill 到 ~/.claude/skills/media-review/
├── skill/media-review/
│   ├── SKILL.md       # 主技能
│   └── scripts/fetch_bili.py
├── docs/design.md     # 本文档同步副本
├── tests/fixtures/    # 录制的 B站 API 响应，测诊断逻辑不真连
└── .gitignore
```

## 五、数据输入

- **B站自动轨**：`fetch_bili.py` 用 Cookie 拉创作中心投稿列表（首次全量盘点历史投稿，之后增量）+ 单视频数据；自动按发布日期算 D+3/D+7/D+30 是否到期。Cookie 失效直接报错提示更新，不猜。API 失败重试一次，仍失败降级为手动粘贴模式
- **手动轨（抖音/小红书）——B站驱动+主动问+截图收件箱**（2026-09-16 定稿）：
  - **主轴**：B站到期内容为锚，skill **主动问用户**"这期抖音/小红书发了吗"，用户找截图贴收集窗；登记表不是必须
  - **收集端**：收集窗口 `scripts/screenshot_tray.py`（tkinter+Pillow，置顶小窗）——**复盘时由 skill 启动，非常驻**（无托盘/无自启/无热键）。截图后点窗 Ctrl+V 连续贴多张；**期数输入框 + 平台下拉开子目录**：`_截图收件/<期数>/<平台>_<时间戳>.png`，空期数归 `未分期/` 由 AI 后整理归期
  - **AI 配期**：读图识别标题 → 与 B站视频标题模糊匹配验证期数，配不上的问用户
  - **抖音 Excel 导出**：复盘时给文件路径，直接读
- 发布 <3 天的内容只登记不复盘

## 六、诊断框架（方法论映射）

爆款公式：**爆款 = 选题 × 素材 × 内容**（[[30_方法论/自媒体方法论体系|方法论总纲]] §2），复盘 = 对已发布内容做三要素归因：

| 要素 | 数据信号（B站创作中心） | 归因 |
|---|---|---|
| 选题 | 播放量 vs 粉丝基线、CTR、播放来源分布 | 播放低 = 选题没踩中 or 封面标题不行 |
| 素材 | 赞/播比、评论区"我也遇到过"类共鸣占比 | "自己的东西"够不够独特 |
| 内容 | 完播率、评论流失点 | "爱听的方式"行不行 |
| 对标 | 同选题 vs [[20_自媒体/对标]] 笔记里的数据 | 差距在哪、抄什么操作 |
| 跨平台 | 同一期三平台播放/互动率/涨粉相对表现（汇总报告合参） | 某平台独好 → 内容偏该平台调性，下期封面标题按平台适配 |

输出：三要素各一句话结论 + 跨平台一句话结论 + **下条改动最多 3 条**（可执行，不写空话）+ 建议写回清单。

## 七、报告与写回

- 报告：`20_自媒体/复盘/YYYY-MM-DD_期数_标题.md`，**一期一份**，D+3/D+7/D+30 追加数据快照段落，不新建。内含：改进实验核对 → 三平台数据同表 → 跨平台对比 → 三要素归因 → 对标校准 → 下条改动（进改进实验）→ 建议写回。frontmatter 按 vault 规范，加 `期数` 字段
- 改进闭环：报告"下条改动"逐条登记 `_改进实验.md`（改动/假设/可判定验证标准），下期复盘开头核对验证结果：有效→用户确认后沉淀复用片段+标已验证；无效→标注分析。这是方法论"验证机制"的实例化
- 写回：报告末尾"建议写回"清单（checkbox），用户确认后 skill 写入选题池/复用片段/对标笔记，写完勾掉留痕

## 八、错误处理与测试

- Cookie 失效 → 明确报错；<3 天 → 只登记；API 挂 → 重试一次→降级手动
- `fetch_bili.py --dry-run` 不写盘；诊断逻辑用 fixtures 测，不真连 B站

## 九、里程碑

- **M1（MVP，✅ 2026-09-16 完成并实测）**：B站单条复盘全自动 + 手动平台截图收件箱 + 报告 + 半自动写回；实测跑通 B站轨（15 条投稿）
- **M1.5（期驱动+闭环，✅ 2026-09-16 完成）**：按"期"复盘、主动问多平台、收集窗分平台分期数、三平台合参汇总报告、改进实验闭环（_改进实验.md）
- **M2**：账号周期复盘（月度报告），吃 M1/M1.5 攒的报告
- **M3（可选）**：Codex automation 定时套壳；抖音官方 API

## 十、开放问题

1. B站创作中心接口的具体字段与 wbi 签名——实现阶段对着 bilibili-API-collect 文档确认
2. 完播率等数据在创作中心接口里的可得性——同上，拿不到就在报告里标注"数据不可得"

## 相关

- [[30_方法论/自媒体方法论体系]] — 诊断框架的理论来源
- [[20_自媒体/对标/_综合对比]] — 对标校准的数据源
- [[CLAUDE.md|项目 CLAUDE]] — 项目入口
